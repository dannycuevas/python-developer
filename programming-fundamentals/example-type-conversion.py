# - Example storing a value as an integer
#	- then creating a new number, using the first integer plus "1"
#	- finally, printing the final result as an output

number = int(input('Please enter a number: '))
new_number = number + 1
print(f'The result is: {new_number}')
